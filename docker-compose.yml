version: '3.8'

services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - '3000:3000'
    volumes:
      - sqlite_data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_URL=/app/data/db.sqlite

volumes:
  sqlite_data:
